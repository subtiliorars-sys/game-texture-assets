# TEXTURE GO-BIG

Local vendor ~12.5 GB of CC0 maps.

GitHub may host this as **Release zip batches** (see Releases) if git clone of the full binary tree is too heavy.
Re-fetch anytime with `scripts/fetch-*-parallel.ps1`.
